package notes14Collections;

public class NotesHashMap {
    //hasmaps-(App.lesson 15)
    //put to add. order will not be as put in you did aka no order.
        //key can be used as index if you need an index
        // Add - ads to end but put just puts in the bag
    //input as a key & value
        //key must be unique but value can be repeated
        //if add another with same key it replaces the old value for new one but still only one key
}
